name: Notion Backup

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * *"

permissions:
  contents: write

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run backup
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: |
          python scripts/backup_notion.py --output backup

      - name: Stage backup for data branch
        run: |
          rm -rf /tmp/notion-backup
          mkdir -p /tmp/notion-backup
          cp -a backup /tmp/notion-backup/backup

      - name: Commit and push to data branch if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin data || true
          if git show-ref --verify --quiet refs/remotes/origin/data; then
            git checkout -B data origin/data
          else
            git checkout -B data
          fi
          git rm -r --ignore-unmatch backup
          cp -a /tmp/notion-backup/backup ./backup
          git add backup
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore: backup notion workspace"
          git push -u origin data
